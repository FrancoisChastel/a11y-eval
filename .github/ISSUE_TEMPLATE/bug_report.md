---
name: Bug report
about: Something evaluated wrongly, crashed, or misbehaved
labels: bug
---

**What happened**

**Command run** (full CLI invocation, or the `evaluate()` call)

```bash

```

**Expected vs actual**

**Environment**
- a11y-eval version / commit:
- Node version:
- OS:
- Target app framework (if repo mode):

**report.json excerpt or stderr** (if relevant — redact URLs you can't share)

```json

```

If the bug is a **false positive/negative from an engine**: which rule (`ruleId`), and a minimal HTML snippet that reproduces it. Minimal repro pages can become permanent fixtures in `test-fixtures/` — those are the best bug reports.
